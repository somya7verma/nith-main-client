import AdminLayout from '@/components/ui/admin-layout';
import Credentials from '@/components/home-components/credentials/credentials';

export default function CredentialsPage() {
  return (
    <AdminLayout>
      <Credentials />
    </AdminLayout>
  );
}
